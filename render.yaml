services:
  - type: web
    name: sage-finance
    runtime: python

    buildCommand: "pip install -r requirements.txt"

    startCommand: "gunicorn -b 0.0.0.0:$PORT bot:app"

    envVars:
      - key: TOKEN
        sync: false
